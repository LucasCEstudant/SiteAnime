services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "5173:80" # maps container's nginx(80) to host 5173
    restart: unless-stopped
    environment:
      - NGINX_HOST=0.0.0.0
