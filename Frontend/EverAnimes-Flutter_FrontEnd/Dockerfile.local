# Dockerfile.local — usar quando ja buildar localmente com:
#   flutter build web --release
# Apenas copia build/web para nginx. Build em segundos.
FROM nginx:alpine
COPY build/web /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
